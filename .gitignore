# 敏感/私人文件(绝不推送)
providers.json  # 用户设置(提供方/模型), 不推送不被覆盖
keys.json
prefs.json
*.db
media/
server.log
server.pid
__pycache__/
*.pyc
.env

# --- Android / Capacitor (由 CI 生成, 不入库) ---
node_modules/
www/
android/
ios/
package-lock.json

# CI 状态回写文件(由工作流生成)
.ci/
